import Button from './Button';

const meta = {
  component: Button,
};

export default meta;

export const Default = {
  args: {
    label: "Let's Shop"
  }
};

export const BuyNow = {
  args: {
    label: "Buy Now"
  }
};