import Footer from './Footer';

const meta = {
  component: Footer,
};

export default meta;

export const Default = {};

export const FooterSubscribeNow = {
  args: {
    title: "Subscribe Now"
  }
};